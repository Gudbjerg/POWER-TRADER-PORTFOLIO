# Secrets — never commit these
.env
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
env/

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Runtime state — excluded from main, but committed on hf-deploy for cold-start performance
# (see hf-deploy branch which removes these exclusions)
data/generation_cache.csv
# model weights excluded on hf-deploy: HF rejects binary files (Xet policy)
# weights are committed on main for Streamlit Cloud live inference
models/weights/*.pt
models/weights/*.joblib

# Project-specific notes (personal context files)
.extra_context.md
.prompt.md
1st_prompt.md
extra_context.md
POWER\ TRADER\ PORTFOLIO.md
FUTURE_PLANS.md
